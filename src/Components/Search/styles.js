import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    results: {
        display: 'flex',
        flexWrap: 'wrap',
        justifyContent: 'center',
        padding: '2rem',
        flexFlow: 'row'
      }
}));