import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  pets: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
    padding: "2rem",
    flexFlow: "row",
  },
  title: {
    justifyContent: "center",
  },
}));
